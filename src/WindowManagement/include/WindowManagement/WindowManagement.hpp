//
// Created by Sebastian Borsch on 27.12.24.
//

#pragma once

/**
 * @class WindowManagement
 * @brief Main interface with the window management system
 *
 * Provides core functionality to the library.
 */
class WindowManagement{
	public:
		/**
		 * @brief Constructor of the Window Management
		 */
		WindowManagement();

		/**
		 * @brief Destructor of the Window Management
		 */
		~WindowManagement();

		/**
		 * @brief Example function to do something
		 * @param val a test value to work with
		 * @return Result of doing nothing
		 */
		int foo(int val);

		/**
		 *
		 * @param a a is a
		 * @param b b is b
		 * @param mac
		 * @return return motherfucker!
		 */
		float bar(int a, int b, float mac);
};